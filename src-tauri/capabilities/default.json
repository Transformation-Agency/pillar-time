{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-local-pillar-time",
  "description": "Allows the main Pillar Time desktop webview to check, install, and restart into signed app updates.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:*", "http://localhost:*"]
  },
  "permissions": ["core:default", "updater:default", "process:allow-restart"]
}
