# Pillar Time environment example
#
# Credentials can also be added inside Settings. Environment variables are useful
# for self-hosted deployments, containers, or desktop development.

# Server
HOST=127.0.0.1
PORT=42818
PILLAR_TIME_APP_MODE=web
PILLAR_TIME_DATA_DIR=./data
# PILLAR_TIME_DB_PATH=./data/pillar-time.sqlite

# Model providers
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OPENROUTER_API_KEY=
GEMINI_API_KEY=
XAI_API_KEY=

# Optional shared Google Calendar OAuth client.
# Desktop uses the installed-app PKCE flow and does not need a client secret.
# Self-hosted/custom deployments may override these if they own a separate client.
# PILLAR_TIME_GOOGLE_CALENDAR_CLIENT_ID=190790037747-8mou84ivna7taems83u92t7fpfsd12m3.apps.googleusercontent.com
# PILLAR_TIME_GOOGLE_CALENDAR_CLIENT_SECRET=

# Linear project connector.
# Personal API key only. The app reads this from the launch environment and
# does not store it in SQLite.
LINEAR_API_KEY=
# RUN_LINEAR_SMOKE=0

# Custom OpenAI-compatible provider
CUSTOM_MODEL_API_KEY=
# CUSTOM_MODEL_BASE_URL=http://localhost:11434/v1

# Optional podcast transcription override
# OPENAI_TRANSCRIPTION_MODEL=gpt-4o-mini-transcribe
# FFMPEG_PATH=/opt/homebrew/bin/ffmpeg

# Optional local speech-to-text with whisper.cpp
# WHISPER_CPP_PATH=/opt/whisper.cpp/build/bin/whisper-cli
# WHISPER_MODEL_PATH=/opt/whisper.cpp/models/ggml-tiny.en.bin
# WHISPER_MODEL_NAME=tiny.en
