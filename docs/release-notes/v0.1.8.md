# Pillar Time v0.1.8

Current-main release refresh.

## Fixes

- Publishes a fresh signed/notarized build from the current `main` release flow.
- Keeps Pillar Time on its dedicated backend port so it can run alongside Pillar Brief without competing for the same local server.
- Includes the latest release workflow documentation and asset naming guardrails.

## Downloads

- Apple Silicon Mac: `Pillar.Time_0.1.8_aarch64.dmg`
- Intel Mac: `Pillar.Time_0.1.8_x64.dmg`
- Windows: `Pillar.Time_0.1.8_x64-setup.exe`
